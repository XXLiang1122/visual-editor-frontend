import { createContext } from "react";
export const ScaleContext = createContext<number>(1)
export const BackgroundContext = createContext<any>(null)
export const MovingContext = createContext<any>(null)